# smart-supermarket

Configuração do ambiente de desenvolvimento:

Ubuntu:
``` bash
sudo apt install build-essential
```

Compilando servidor:
``` bash
make server
```

Executando servidor:
``` bash
make serve
```